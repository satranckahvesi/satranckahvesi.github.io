name: Check content links

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-content-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ruby scripts/check_content_links.rb
